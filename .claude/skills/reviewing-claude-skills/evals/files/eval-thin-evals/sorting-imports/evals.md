# Evals

Run the skill on a file whose imports are unordered and confirm the three groups come out in
order. Then run it on a file that mixes type-only and value imports and confirm both survive the
rewrite.
