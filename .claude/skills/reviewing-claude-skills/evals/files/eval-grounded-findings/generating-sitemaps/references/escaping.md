# Escaping

Escape `&` as `&amp;`, `<` as `&lt;`, and `>` as `&gt;` in every URL before writing it, because
an unescaped entity makes the XML unparseable for every consumer, not just the affected URL.
